import React from "react";
import styled from "styled-components";

const TopContainer = styled.div`
  width: 100%;
  height: 100vh;
  padding: 0;
  background-color: grba(255, 255, 53, 0.88);
`;

const TopSectiton = () => {
  return <TopContainer></TopContainer>;
};

export default TopSectiton;
